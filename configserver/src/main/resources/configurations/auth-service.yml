server:
  port: 9090
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/E-Com
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect